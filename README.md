# mini-wallet
```
please fill .env.sample file as .env file
project need postgres install on local or can use https://www.elephantsql.com/ for free server
run .sql file query on server before using service

python3 -m venv env
source env/bin/activate
pip3 install -r requirements.txt

uvicorn app.server:app --reload --port 8080
```
